<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itsamankrsingh.facebooksignin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FacebookSignIn">
        <activity
            android:name=".ui.GoogleLoginActivity"
            android:label="@string/title_activity_google_login"
            android:theme="@style/Theme.FacebookSignIn.NoActionBar"></activity>
        <activity
            android:name=".ui.fragments.GoogleLoginActivity"
            android:label="@string/title_activity_google_login"
            android:theme="@style/Theme.FacebookSignIn.NoActionBar" />
        <activity
            android:name=".ui.FacebookLoginActivity"
            android:label="@string/title_activity_facebook_login"
            android:theme="@style/Theme.FacebookSignIn.NoActionBar" />
        <activity
            android:name=".WelcomeActivity"
            android:label="Welcome"
            android:parentActivityName=".ui.MainActivity"
            android:theme="@style/Theme.FacebookSignIn.NoActionBar" />
        <activity
            android:name=".ui.MainActivity"
            android:label="Sign In">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
    </application>

</manifest>